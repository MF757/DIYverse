# Copy this file to .env in the same folder (project root). Restart the dev server after changing .env.

# Supabase (backend / server-side)
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
# Optional: for running apply-discover-migration script. Dashboard → Project Settings → Database → Connection string (URI)
# SUPABASE_DB_URL=postgresql://postgres.[ref]:[YOUR-PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres

# Frontend (Vite) – required for the React app; use same URL and anon key as above.
# Get these from Supabase Dashboard → Project Settings → API.
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Google AdSense (optional). If set, one ad slot is shown per 25 projects on the home feed.
# See docs/ADSENSE_SETUP.md for how to get these values and link your site.
# VITE_ADSENSE_CLIENT_ID=ca-pub-XXXXXXXXXXXXXXXX
# VITE_ADSENSE_SLOT_ID=1234567890

# Google Search Console (optional). Verification code from the HTML tag method; used to inject the meta tag.
# See docs/SEARCH_CONSOLE_SETUP.md. Sitemap base URL is set by VITE_APP_URL at build time.
# VITE_GOOGLE_SITE_VERIFICATION=your-verification-code-from-search-console
# VITE_APP_URL=https://yourdomain.com
