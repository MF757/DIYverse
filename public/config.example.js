// Optional runtime config: copy to config.js and set your Supabase URL and anon key.
// Alternatively use .env with VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY (then restart dev server).
// Get values from Supabase Dashboard → Project Settings → API.
window.SUPABASE_URL = 'https://your-project.supabase.co';
window.SUPABASE_ANON_KEY = 'your-anon-key';
